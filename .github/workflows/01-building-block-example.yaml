name : Building Block Example
on:
  push:
    branches:
      - main

jobs:
    echo-hello-world:
        runs-on: ubuntu-latest
        steps:
            - name: Echo Hello World
              run: echo  "${{ github.event_name }}" 
   